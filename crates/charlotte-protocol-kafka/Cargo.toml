[package]
name = "charlotte-protocol-kafka"
version = "0.1.0"
edition = "2024"
description = "CharlotteOS capability-oriented Kafka service wire protocol"

[dependencies]
charlotte-launch = { path = "../charlotte-launch" }
