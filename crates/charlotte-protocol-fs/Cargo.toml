[package]
name = "charlotte-protocol-fs"
version = "0.1.0"
edition = "2024"
description = "Native CharlotteOS filesystem protocol on top of the persistent object store"
license = "AGPL-3.0-or-later"

[dependencies]
