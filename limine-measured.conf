# Opt-in development configuration for firmware that exposes TPM2/CC
# measurement services. Limine measures the config and kernel into PCRs 8/9.
TIMEOUT: 0
SERIAL: yes
MEASURED_BOOT: yes

/Catten
    PROTOCOL: limine
    KERNEL_PATH: boot():/catten
    KASLR: no
