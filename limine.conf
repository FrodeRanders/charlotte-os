TIMEOUT: 0
SERIAL: yes

# Development configuration. Secure Boot requires a generated configuration
# whose kernel path includes the kernel's BLAKE2B digest, plus config-hash
# enrollment and EFI signing; see docs/guides/limine.md.

/Catten
    PROTOCOL: limine
    KERNEL_PATH: boot():/catten
    KASLR: no
