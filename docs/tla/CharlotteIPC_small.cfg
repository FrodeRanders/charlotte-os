\* Small-config for fast TLC checking. Verifies the core invariants with
\* minimal state space (~500-2000 states, ~10s runtime).
SPECIFICATION Spec

CONSTANTS
    ASID       = {a1, a2}
    MaxCaps    = 6
    MaxEps     = 1
    MaxTokens  = 2
    MaxCalls   = 2
    MaxMems    = 2
    MaxQueue   = 1
    NullAsid   = a0

INVARIANT Invariants

CHECK_DEADLOCK FALSE
