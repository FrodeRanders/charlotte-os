\* Negative regression configuration. Cancellation is a request, not a
\* terminal event, so it must not release the operation's buffer.
SPECIFICATION UnsafeSpec

CONSTANTS
    ASID       = {a1}
    MaxOps     = 2
    MaxCqs     = 1
    MaxRing    = 1
    MaxTimers  = 1
    NullAsid   = a0

INVARIANT NonTerminalBufferRemainsLoaned

CONSTRAINT StateConstraint

CHECK_DEADLOCK FALSE
